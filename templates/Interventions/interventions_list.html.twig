{% extends 'base.html.twig' %}

{% block title %}Interventions
{% endblock %}
{% block body %}
<div>
	{% include "Components/sidebar.html.twig" %}
	{#{include('instructor/instructor_informations.html.twig', { form: form })}#}
	<div class="enseignant-card">

		<h2>
			{{ results[0].prenom }}
			{{ results[0].nom }}
		</h2>

		<h3 class="section-title">Modules enseignés</h3>

		<ul class="modules-list">
			{% for row in results %}
				<li class="module-item">
					<strong>{{ row.module }}</strong>
					:
					{{ row.nbHeures }}h
										                    (dont
					{{ row.nbHeuresRestantes }}
					restantes pour cette année)
				</li>
			{% endfor %}
		</ul>

		<a href="" class="btn-export">
			Exporter le récapitulatif (fichier .xls)
		</a>

	</div>
	<hr>
	<div>
		<button class="btn active" active>
			<a href="{{ path('enseignant_infos', {id: instructor.id}) }}">Informations générales</a>
		</button>
		<a href="{{ path('app_add_course')}}" class="text-blue-950 text-3xl font-medium">Interventions</a>
	</div>
	<br>


	{{ form_start(form) }}

	{{ form_end(form) }}

		<div>
			<h2>Informations générales</h2>

		</div>
		{{ form_start(form) }}

		{{ form_end(form) }}

		<div class="interventions-container">
			{% if pagination|length == 0 %}
				<tr>
					<td colspan="6" class="center">
						Aucune intervention trouvée.
					</td>
				</tr>
			{% else %}
				<h2>
					{{ pagination.getTotalItemCount }}
					intervention{{ pagination.getTotalItemCount > 1 ? 's' : '' }}
					trouvée{{ pagination.getTotalItemCount > 1 ? 's' : '' }}
					au total
				</h2>

				<div class="table-wrapper">
					<table class="interventions-table">
						<thead>
							<tr>
								<th>Date de l’intervention</th>
								<th>Module</th>
								<th>Type</th>
								<th>Intervenants</th>
								<th>En visio</th>
								<th></th>
							</tr>
						</thead>
						<tbody>
							{% for row in pagination %}
								<tr>
									<td>{{ row.startDate|date('d/m/Y H:i') }}</td>
									<td>{{ row.moduleName }}</td>
									<td>{{ row.interventionType }}</td>
									<td>{{ row.intervenants }}</td>
									<td class="center">
										{% if row.remotely %}🎥
										{% endif %}
									</td>
								</tr>
							{% endfor %}
						</tbody>
					</table>
				</div>
			{% endif %}
		</div>
		<div class="pagination">
			{{ knp_pagination_render(pagination) }}
		</div>
	</div>
{% endblock %}
