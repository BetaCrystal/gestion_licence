{% extends 'base.html.twig' %}

{% block body %}
    <button class="btn"><a href="{{ path('liste_interventions') }}">X</a></button>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ path('liste_interventions') }}" class="btn-danger">Annuler</a>
    <button type="submit" class="btn btn-primary">Enregistrer les informations</button>
    {{ form_end(form) }}

    <form method="post" action="{{ path('app_course_delete', {'id': course.id}) }}">
        <input type="hidden" name="_token" value="{{ csrf_token('delete_course') }}">
        <button type="submit" class="btn btn-danger">Supprimer</button>
    </form>
{% endblock %}
