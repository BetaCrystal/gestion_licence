{% extends 'base.html.twig' %}

{% block title %}Form Interventions{% endblock %}

{% block body %}

    <button class="btn"><a href="{{ path('app_course') }}">Retour à la liste des interventions</a></button>
    <form method="post">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn"> {{ button_label|default('Annuler') }} </button>
        <button class="btn">{{ button_label|default('Confirmer') }}</button>
        {{ form_end(form) }}

    </form>


{% endblock %}