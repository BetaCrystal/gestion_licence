<form method="post" action="{{ path('app_delete_schoolyear', {'id': schoolYear.id}) }}" onsubmit="return confirm('Confirmez-vous la suppression ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete_schoolyear' ~ schoolYear.id) }}">
	<button type="submit" class="btn btn-danger">Supprimer</button>
</form>
